export default function EventFilters({filters,onChange,categories=[]}){ return <div className='card p-4 filters'><input className='input-field' placeholder='Search events...' value={filters.search||''} onChange={(e)=>onChange({...filters,search:e.target.value})}/><select className='input-field' value={filters.categoryName||''} onChange={(e)=>onChange({...filters,categoryName:e.target.value})}><option value=''>All categories</option>{categories.map((c,i)=><option key={c.id??i} value={c.categoryName??c.name}>{c.categoryName??c.name}</option>)}</select></div>; }
